## Functions

zsh restic includes a small utility `restic` to fetch and install new packages.

#### `restic::install`

Install restic.

#### `restic::load`

Load path restic.

#### `restic::upgrade`

Upgrade version restic.
